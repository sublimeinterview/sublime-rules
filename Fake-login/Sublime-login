<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sublime Security - Secure Access</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #eef2ef;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .container {
            background: white;
            padding: 40px;
            width: 360px;
            border-radius: 10px;
            box-shadow: 0 4px 25px rgba(0,0,0,0.12);
            border-top: 6px solid #0c6b38;
        }

        .brand {
            text-align: center;
            font-size: 22px;
            font-weight: 800;
            color: #0c6b38;
            margin-bottom: 5px;
            letter-spacing: 0.7px;
        }

        .brand-subtitle {
            text-align: center;
            font-size: 12px;
            color: #444;
            margin-bottom: 15px;
        }

        .simulation {
            text-align: center;
            font-size: 12px;
            margin-bottom: 15px;
            color: #b33a3a;
            font-weight: bold;
        }

        h2 {
            margin-top: 0;
            font-weight: 600;
            color: #222;
            text-align: center;
        }

        label {
            font-size: 14px;
            color: #333;
            display: block;
            margin-bottom: 6px;
            margin-top: 18px;
        }

        input[type="email"], input[type="password"] {
            width: 100%;
            padding: 12px;
            border: 1px solid #c6c6c6;
            border-radius: 4px;
            font-size: 14px;
            box-sizing: border-box;
        }

        button {
            width: 100%;
            padding: 12px;
            margin-top: 28px;
            background: #0c6b38;
            color: white;
            border: none;
            border-radius: 4px;
            font-size: 15px;
            cursor: pointer;
            transition: background 0.2s;
        }

        button:hover {
            background: #09582d;
        }

        .footer-msg {
            margin-top: 20px;
            font-size: 12px;
            color: #666;
            text-align: center;
        }
    </style>
</head>
<body>

<div class="container">

    <div class="brand">Sublime</div>
    <div class="brand-subtitle">Security</div>

    <h2>Account Login</h2>

    <form onsubmit="event.preventDefault(); alert('Training Simulation Only. No credentials collected.');">
        <label for="email">Email Address</label>
        <input type="email" id="email" placeholder="name@example.com" required />

        <label for="password">Password</label>
        <input type="password" id="password" placeholder="Enter your password" required />

        <button type="submit">Sign In</button>
    </form>

    <div class="footer-msg">
        This page is a simulation for training and demonstration only.
    </div>
</div>

</body>
</html>
